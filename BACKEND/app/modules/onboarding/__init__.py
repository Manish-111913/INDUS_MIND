"""Onboarding module (docs/05 S10) — guided tours, changelog, sample-data loading.

Even the product tour isn't hardcoded: steps come from `tours`/`tour_steps`, and
"What's new" from `changelog_entries`, so both are editable without a deploy.
"""
