"""Integration layer (docs/05 S8) — API keys + outbound webhooks.

The machine-to-machine surface: an API key is an alternative principal carrying
its own scopes, and webhook endpoints subscribe to domain events so an external
system (SAP PM, Maximo) can react to failures and work orders.
"""
