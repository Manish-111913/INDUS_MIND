"""Content pages (docs/08 N5) — privacy, terms, and other markdown pages.

Even legal text isn't hardcoded: it lives in `content_pages`. Pages marked public
are fetchable pre-login so the landing/login footer can link them.
"""
