"""Tenant module events (docs/02 §34). Provisioning events land as the module grows."""

from __future__ import annotations

# Reserved: tenant.created / tenant.suspended published via app.core.events.bus.
